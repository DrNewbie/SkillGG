_G.ATKUp340WarCry = _G.ArrowRain600WarCry or {}
_G.SkillGGSystem = _G.SkillGGSystem or {}

if not Utils or not Utils:IsInHeist() then

else
	managers.player:add_to_temporary_property("ArrowRain600WarCry", 1, 1)
end
